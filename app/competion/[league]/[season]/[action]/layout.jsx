import CompetionContent from "../../../../components/mainSection/CompetionData/CompetionContent"

const layout = ({ children }) => {
  return <CompetionContent>{children}</CompetionContent>
}

export default layout
